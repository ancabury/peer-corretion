%h2.span5
  Paper
= render 'paper_info'

%div.btn-group.read-paper-btn
  = link_to 'Read paper'.html_safe, user_user_paper_download_path(current_user, @paper.id), class: "btn btn-primary"

.clear
Before you grade a paper, you have to read it !

.clear
%h3.span5
  Grade Paper
%div.pull-left
  = render partial: "form", locals: {url: user_user_correction_path(current_user, @correction.id), method: "update"}
