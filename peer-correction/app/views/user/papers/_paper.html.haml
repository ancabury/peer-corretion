%tr
  %td
    = paper.name
  %td
    = paper.user.name
  %td
    = paper.created_at.to_formatted_s(:long)
  %td
    - if paper.user_id == current_user.id
      = paper.grade
    - else
      = ""
  %td
    = link_to '<i class="glyphicon glyphicon-file"></i>View'.html_safe,  "#"
