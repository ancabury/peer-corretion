%h2.span5
  Paper
= render 'paper_info'

%div.btn-group
  = link_to 'Read paper'.html_safe, user_user_paper_path(current_user, @paper.id), class: "btn btn-primary"

%h3.span5
  Grade Paper
%div.pull-left
  = render partial: "form", locals: {url: user_user_correction_path(current_user, @correction.id), method: "update"}
